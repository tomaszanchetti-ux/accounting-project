[build]
watchPatterns = [
  "backend/**",
  "data/demo_seed/**",
  "Dockerfile",
  "railway.toml",
]

[deploy]
healthcheckPath = "/health"
healthcheckTimeout = 60
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
